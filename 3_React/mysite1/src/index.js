import React from "react";
import ReactDOM from "react-dom";
import WebForms from "./WebForms/WebForms";

const rootElement = document.getElementById("root");
ReactDOM.render(<WebForms/>, rootElement);