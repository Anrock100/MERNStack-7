// create login function
// get login info (user, and password) as a parameter
// compare login info on database collection
// if found return true else return false

var login=(userInfo)=>{
        var {user, pass } = userInfo;
        //connect with database
        //create schema
        //create model
        //search/compare user, and pass on respective collection of database
        // if found return true else return false
}